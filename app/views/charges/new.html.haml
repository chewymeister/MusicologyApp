%h2
  Please confirm your details

- if current_user.addresses
  .address
    %h3
      Address
    = @address.addressee
    = @address.lineOne
    = @address.lineTwo
    = @address.town
    = @address.county
    = @address.postcode

- if current_user
  .email
    %h3
      Email
    = current_user.email

.email
  = current_user.email

= form_tag charges_path do
  %article
    %label(class="amount")
      %span
        Amount: £5.00

  - data_key = "#{ Rails.configuration.stripe[:publishable_key] }"
  %script(src="https://checkout.stripe.com/v2/checkout.js" class="stripe-button"
          data-key = data_key
          data-description = "A month's subscription"
          data-amount="500"
          data-currency="gbp")