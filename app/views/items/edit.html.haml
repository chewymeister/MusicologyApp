= form_for @item, html: {multipart: true} do |f|
  = f.label :name
  = f.text_field :name

  = f.label :price
  = f.text_field :price

  = f.label :description
  = f.text_field :description

  = f.label :image
  = f.file_field :image

  = f.submit 'Edit item'